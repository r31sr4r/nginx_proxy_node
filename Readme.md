# Utilizando Nginx como proxy reverso para aplicação Node.js / Mysql

## Passos:

    - Clonar o repositório;
    - Rodar o comando:

    ``` $ docker-compose up -d ```

    - Acessar o endereço http://localhost:8081